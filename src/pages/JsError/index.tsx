import { useLocation } from 'react-router-dom';
import type { FC, ReactElement } from 'react';

const JsErrror: FC = (): ReactElement => {
  // console.log(useLocation());

  return <div>JsErrror</div>;
};

export default JsErrror;
